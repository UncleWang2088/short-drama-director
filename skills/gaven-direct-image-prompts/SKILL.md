---
name: gaven-direct-image-prompts
description: 将简短、口语化或不完整的自然语言画面需求，整理并补全为结构规范、可直接复制的中文文生图正向提示词。用于“转成图片提示词”“规范画面描述”“补全景别、构图、主体、动作、环境、美学或调度”，以及选择导演视觉语言、摄影风格、暖金贵妃夜宴风、暖金贵妃夜宴风02、胶片模拟、Kodak 2383、Fujifilm 3513 或风格强度的人物、产品、场景、插画和概念设计任务。视频提示词、参考图锚定、图生图编辑、局部改图和 Neo Image 请求交由对应专用技能处理。
---

# Gaven 图像提示词导演

把用户的自然语言画面需求转换为一条模型无关、中文、可直接复制的文生图正向提示词。保持原意，合理补全视觉信息，让所有描述服务于同一个画面意图。

## 工作流程

1. 提取用户明确给出的主体、数量、动作、环境、时代、风格、色彩和镜头要求，并将其作为最高优先级约束。
2. 判断画面类型：人物、产品、场景、插画、概念设计或混合类型。
3. 根据画面意图补全景别、观察角度、构图、空间关系、环境层次、光色和材质。
4. 将抽象感受翻译成可见、可拍、可渲染的视觉特征。
5. 按固定顺序组织成品：
   `画面类型 → 景别与机位 → 构图 → 主体 → 动作与表情 → 空间调度 → 环境层次 → 光影与色彩 → 美学与材质`（画幅比例禁止写入，见输出格式画幅铁律）
6. 输出前逐项核对人物携带物、职业装备、品牌、标签、文字、核心道具与叙事事件，确认每个实体都能在用户输入中找到来源。
7. 直接输出一条完成度最高的提示词。

## 补全原则

- 完整保留用户明确的信息；角色数量、随身物件、职业装备、品牌状态、文字状态、核心道具、知识产权元素和叙事事件均以用户原话为唯一来源。
- 随身物件、职业装备、品牌、标签和包装文字仅在用户明确提及时写入成品。
- 人物与产品信息缺省时，仅描述用户给出的身份或品类以及完成画面所需的姿态、外形、材质和光影。例如将“独行者”写为“一名穿着御寒服装的独行者”，将“香水”写为“一瓶造型精致的透明玻璃香水瓶”。
- 情绪依据用户原话呈现；信息缺省时采用与动作一致的自然表情和姿态。
- 情绪与表情画面优先近景，人物动作优先中景或全景，产品质感优先特写，环境叙事优先大全景。
- 单主体明确位置与留白；双主体明确左右、高低、朝向和视线；多主体明确主次、遮挡与深度顺序。
- 环境按照前景、中景、背景建立空间层次。
- “高级、震撼、孤独”等抽象词通过光线、色彩、尺度、留白、材质和姿态具体呈现。
- 画幅比例、分辨率、比例参数一律不写入提示词（画幅由用户在生图工具界面选择）。

## 静态调度

“调度”表示单张图片中的空间组织：

- 主体在画面左、中、右或上下区域的位置。
- 身体朝向、头部朝向和视线落点。
- 主体之间的距离、相对高低、主次和遮挡关系。
- 前景、中景、背景的内容与深度顺序。
- 动作冻结在最具辨识度的瞬间，并呈现衣物、头发、液体、烟尘等受力结果。

使用静态画面语言描述最终定格瞬间。视频生成、时间轴、运镜、首尾帧和动作分段任务交由视频类技能处理。

## 画风系统

画风由以下层级组成：

- `Dxx`：导演／摄影师视觉语言，涵盖构图、镜头、调度、光线、色彩、场景、材质和后期气质。需要导演风格或画风菜单时读取 [director-styles.md](references/director-styles.md)。
- `Pxx`：通用摄影风格。需要自然纪实、黄金时刻、商业静物、美妆、硬闪、黑色电影、暖金贵妃夜宴风或暖金贵妃夜宴风02等摄影风格时读取 [photo-styles.md](references/photo-styles.md)。
- `Cxx`：拍摄胶片，涵盖光源倾向、宽容度观感、颗粒、肤色和基础色彩响应。需要胶片模拟或指定胶片时读取 [capture-films.md](references/capture-films.md)。
- `Rxx`：电影正片／印片风格，涵盖最终对比、黑位、高光、色彩分离和影院拷贝质感。需要 Kodak 2383、Fujifilm 3513 或印片风格时读取 [print-films.md](references/print-films.md)。
- `S1–S3`：轻度、标准、强烈。用户未指定时采用 `S2`。

组合规则：

- `Dxx` 与 `Pxx` 作为主视觉语言，默认选择其中一项。
- `Cxx` 与 `Rxx` 可独立使用，也可叠加在主视觉语言上。
- 支持 `D01+C04+R01+S2` 等代码输入；成品把代码展开成自然视觉语言。
- 用户说“画风菜单”或“帮我选画风”时，提供最多三组适配当前画面的组合及简短理由；用户选定后再按标准格式输出成品。
- 普通提示词转换直接根据用户意图完成画面，无需主动展示菜单。

## 导演姓名

- 稳定模式为默认：导演姓名用于检索预设，成品使用拆解后的视觉语言。
- 锚点模式：在成品开头使用“受某导演／摄影师相关影像语言启发”，并继续描述具体的构图、调度、光色和材质。
- 用户明确指定姓名呈现方式时采用用户要求。
- 导演预设控制视觉形式，画面主体、环境和事件继续以用户原始需求为中心。

## 冲突处理

- 优先寻找能够同时满足要求的静态构图。
- 两个核心要求无法在同一画面成立时，先询问一个决定性问题，确认后再生成成品。
- “面部特写且完整展示全身”“纯俯视且完整呈现正面五官”等组合属于需要确认的高影响冲突。

## 输出格式

每次转换完成后，直接输出一条中文正向提示词。

提示词按照以下顺序连续组织：

`画面类型 → 景别与机位 → 构图 → 主体 → 动作与表情 → 空间调度 → 环境层次 → 光影与色彩 → 美学与材质`

> **画幅铁律**：画幅比例（16:9/9:16/方幅等）、分辨率、比例参数一律**禁止写入提示词**——画幅是生图工具界面上的可选参数，提示词只写内容（场景/角色/动作/光线/表演/材质）。

输出要求：

- 输出从提示词正文直接开始。
- 使用【画面类型】【景别与机位】等短标签区分模块。
- 各模块使用分号自然衔接，形成一条完整提示词。
- 内容聚焦于正向、可见、可执行的画面描述。
- 事实描述采用肯定句式，直接呈现画面中实际可见的主体、环境、材质和光线。
- 使用中文和通用图像生成语言。
- 每次提供一个完成度最高的版本。
- 排他性需求转换为“画面仅呈现……”“背景保持纯净”“主体独立置于……”等正向结果。
- 用户要求简短时压缩措辞，同时保留主体、镜头、构图、调度、环境、光色和美学。
- 用户需求存在关键冲突时，先询问一个最关键的问题，确认后再输出成品提示词。

## 示例

输入：`雨夜，一个女孩等公交。`

输出：

```text
【画面类型】电影感都市人物场景；【景别与机位】中远景，平视视角；【构图】三分法构图，女孩位于画面右侧，左侧保留街道与公交驶入方向的空间；【主体】一名独自等车的年轻女孩；【动作与表情】安静站在站牌下，双手自然收拢，神情平静专注；【空间调度】女孩朝向道路，视线望向画面左侧远处，前景是被雨水打亮的路面，中景是女孩与站牌，背景是虚化的城市灯光；【环境层次】持续细雨，路面积水反射灯光，远处建筑与车灯被雨幕柔化；【光影与色彩】冷蓝雨夜为主色，暖黄色站台灯从侧上方照亮人物面部与肩部，形成冷暖对比；【美学与材质】写实电影摄影质感，真实湿润织物、皮肤与路面反光，克制细腻。
```
